# ESP Studio does not require local `.env` for the SPA.

# Continuous Deployment FTP credentials are GitHub Actions Secrets only:
#   FTP_HOST
#   FTP_PORT
#   FTP_USER
#   FTP_PASSWORD
#   FTP_REMOTE_PATH
#
# See docs/features/deployment.md — never commit real FTP credentials.
