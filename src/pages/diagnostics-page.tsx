import type { JSX } from "react";

import { DiagnosticsPage } from "@/features/diagnostics";

/**
 * Route entry for Device Diagnostics.
 */
export function DiagnosticsRoutePage(): JSX.Element {
  return <DiagnosticsPage />;
}
